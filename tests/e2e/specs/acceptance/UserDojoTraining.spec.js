describe('Dojo training', () => {
    it('a user can train new items', () => {

    });

    it('a user is prompted to review only those items that are due today', () => {

    });

    it('a user can train any set they have previously learned', () => {
        
    });

    it('allows a user to select any set to learn (set access is not progress-gated)', () => {

    });
});