describe('User management', () => {
    it('a guest can create a new account with an email and password', () => {

    });

    it('a user can log in', () => {

    });

    it('a user can log out', () => {
        
    });
});